let array = [2, 5, 1, 3, 4];
let n = 3;

// Ищем индекс элемента n в массиве
for (let i = 0; i < array.length; i++) {
  if (array[i] === n) {
            console.log(`Индекс элемента = ${i}`);

        }
    }
    // Если элемент не найден
console.log("Элемент не найден.");

